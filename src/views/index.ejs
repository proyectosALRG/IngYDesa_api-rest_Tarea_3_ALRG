<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reportes Errores Web</title>
    <!--bootstrap 4 CDN-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>

    <nav class="navbar navbar-dark bg-info  mb-3">
        <a href="/" class="navbar-brand">Reporte de Errores</a>
    </nav>   
    

    <div class="container">
        <div class="row">
        <!--FORM-->
            <div class="col-md-3">
                <div class="card">
                    <div class="card-body">
                        <form action="/guardar" method="POST">
                            <div class="form-group">
                                <input type="text" name="fecha" placeholder="Fecha dd-mm-yyyy" class="form-control">                
                            </div>
                            <div class="form-group">
                                <input type="text" name="codigo" placeholder="Codigo del error" class="form-control">                
                            </div>
                            <div class="form-group">
                                <textarea name="descripcion" class="form-control" placeholder="Descripcion"></textarea>
                            </div>
                            <div class="form-group">
                                <input type="text" name="status" placeholder="Status" class="form-control">                
                            </div>
                            <button type="submit" class="btn btn-success">
                                Guardar
                            </button>
                        </form>
                    </div>

                </div>                    
            </div>    
        <!-- LIST --> 
            <div class="col-md-9">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Fecha</th>
                            <th>Codigo</th>
                            <th>Descripcion       </th>
                            <th>Status</th>
                            <th>Operaciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (var i=0; i<errore.length; i++) { %>
                            <tr>
                                <td><%= i+1 %></td>
                                <td><%= errore[i].fecha %></td>
                                <td><%= errore[i].codigo %></td>
                                <td><%= errore[i].descripcion %></td>
                                <td><%= errore[i].status %></td>
                                <td>
                                    <a href="/editar/<%= errore[i]._id %>" class="btn btn-primary">Editar</a>
                                    <a href="/borrar/<%= errore[i]._id %>" class="btn btn-secondary">Borrar</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
 
                
                </table>
            </div>   
        </div>
    </div>
</body>
</html>
